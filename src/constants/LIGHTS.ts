export enum DIRECTIONAL {
    COLOR = 0xffffff,
    INTENSITY = 1,

    X = 24,
    Z = -32,
    TRANSLATION_Y = 60,
    DAMPING = 12
}

export enum AMBIENT {
    COLOR = 0xffffff,
    INTENSITY = 1
}